let tvShow = [
"The Mr. lesley-Ann",
"Mr. and Mrs. Smith",
"The David Tennant",
"The Broker John"
]
let show = tvShow.find(show =>{
    return show.includes('Mrs')
})
let show2 = show.find(m.indexOF('The') === 0);